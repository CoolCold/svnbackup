# Defaults for svnbackupd initscript
# sourced by /etc/init.d/svnbackupd
# installed at /etc/default/svnbackupd by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
# this is really svnserve arguments, see svnserve(8) for more info
DAEMON_OPTS="--listen-host=127.0.0.1 --listen-port=3690 -r /var/lib/svnbackup --pid-file=/var/run/svnbackupd/svnbackupd.pid -d"

#set RUN to "yes" to enable autostart
RUN="no"
